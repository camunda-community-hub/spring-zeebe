sudo: false
language: java

jdk:
  - oraclejdk8

install: true
cache:
  directories:
    - "$HOME/.m2"

script:
  - "./mvnw clean package verify -B -q"
  - "./mvnw -Danimal.sniffer.skip=true cobertura:cobertura"

after_success:
  - "bash <(curl -s https://codecov.io/bash)"